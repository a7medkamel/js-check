define([
    'lib/underscore',
    'lib/backbone',
    'lib/jquery',
  ],
  function(_, backbone, $) {
    function hello(p1) {
      p1;
    }

    hello();

    var K = 'a';

    $.bad();
    window.$.good()
      
    return function(){
      require([''], function(){
        _;
      });
    };
  });